export struct ProgramOutput {
    stdout: string,
    stderr: string,
    exit_code: int,
}

export global Shell {
    callback execute(string) -> ProgramOutput;
}

export global Command {
    callback execute([string]) -> ProgramOutput;
}
