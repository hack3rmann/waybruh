import { Button } from "std-widgets.slint";
import { Command } from "waybruh/globals.slint";
import { Battery } from "battery.slint";

export component BruhBar inherits Window {
    default-font-size: 16px;

    VerticalLayout {
        alignment: center;
        HorizontalLayout {
            Battery { }
        }
    }
}
